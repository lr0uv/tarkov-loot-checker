name: Auto Update Tarkov Items

on:
  schedule:
    - cron: '0 0 * * *' # 毎日深夜0時（UTC）に自動実行
  workflow_dispatch: # 手動でも実行可能

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Gitへの自動コミット権限を付与
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run update script
        run: node scripts/update-items.js

      - name: Commit and push if changed
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add public/items.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: auto-update items.json [skip ci]" && git push)
